server:
  port: 8081
spring:
  application:
    name: micropersona
  cloud:
    config:
      uri: http://${LOCALHOST}:8080
      username: root
      password: s3cr3t
  boot:
    admin:
      client:
        url: http://${LOCALHOST}:8077