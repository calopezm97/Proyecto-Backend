server:
  port: 8083
spring:
  application:
    name: gatewayservice
  cloud:
    discovery:
      enabled: true
    config:
      uri: http://${LOCALHOST}:8080
      username: root
      password: s3cr3t
