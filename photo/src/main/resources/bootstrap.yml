server:
  port: 8082
spring:
  application:
    name: photo
  cloud:
    config:
      uri: http://${LOCALHOST}:8080
      username: root
      password: s3cr3t
  boot:
    admin:
      client:
        url: http://${LOCALHOST}:8077